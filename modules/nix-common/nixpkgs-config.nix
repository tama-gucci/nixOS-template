{
  ## TODO: uncomment to allow packages with unfree licenses, or leave it commented out to disallow them
  ## the official nix cache (cache.nixos.org) does not serve unfree packages, so the package builds will be done on your machine instead
  # allowUnfree = true;
}
